export enum GymLevel {
  BEGINNER = "beginner",
  INTERMEDIATE = "intermediate",
  PRO = "pro",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum PlanStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  ARCHIVED = "ARCHIVED",
}
